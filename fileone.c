#include<stdio.h>

int main()
{
    printf("this is first remote file edited in github itself");
    printf("Editing fileone.c (this file itself for using push command and uploading it github");
    return 0;
}
